name: presale
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.8.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  storybook_toolkit: ^1.3.2
  csv: ^6.0.0
  cupertino_icons: ^1.0.8
  go_router: ^14.2.0
  flutter_bloc: ^9.0.0
  get_it: ^8.0.3
  collection: ^1.19.1
  intl: ^0.20.2
  dart_date: ^1.4.0
  bot_toast: ^4.1.3
  flutter_list_view: ^1.1.29
  pull_to_refresh: ^2.0.0
  flutter_svg: ^2.0.17
  syncfusion_flutter_xlsio: ^28.2.6

  # storages
  path_provider: ^2.1.5
  flutter_dotenv: ^5.2.1

  # generators annotation
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0
  shared_preferences: ^2.5.2
  dartz: ^0.10.1
  hydrated_bloc: ^10.0.0
  equatable: ^2.0.7
  uuid: ^4.5.1
  provider: ^6.1.2
  # network
  flutter_secure_storage: ^9.2.4
  image_picker: ^1.1.2
  flutter_portal: ^1.1.4
  cached_network_image: ^3.4.1
  universal_html: ^2.2.4
  modal_bottom_sheet: ^3.0.0
  url_launcher: ^6.3.1
  share_plus: ^10.1.4
  uni_links: ^0.5.1
  file_picker: ^9.0.0
  excel: ^4.0.6
  flutter_web_plugins:
    sdk: flutter
  moon_design: ^1.1.0

  storybook_flutter:
    git:
      url: https://github.com/coingaming/storybook_flutter.git
      ref: 506c73296c85a0bb970645036baa1c9213df1c6c
      path: packages/storybook_flutter
  transparent_image: ^2.0.1
  package_info_plus: ^8.0.2
dependency_overrides:
  intl: ^0.18.0
  collection: ^1.19.0
  go_router: ^12.1.1
  pointer_interceptor: ^0.9.3+7
  device_frame:
    git:
      url: https://github.com/bradrushworth/flutter_device_preview.git
      path: device_frame
  device_preview:
    git:
      url: https://github.com/bradrushworth/flutter_device_preview.git
      path: device_preview
dev_dependencies:
  flutter_test:
    sdk: flutter

  freezed: ^2.5.8
  intl_utils: ^2.8.10
  build_runner: ^2.4.15
  json_serializable: ^6.9.4
  flutter_native_splash: ^2.4.4
  flutter_launcher_icons: ^0.14.3
  flutter_lints: ^5.0.0
  storybook_toolkit_tester: ^1.3.0

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/calculator_data/
    - assets/code_snippets/
    - assets/components/
    - assets/images/
    - assets/svg/
    - assets/input/
    - assets/input/стадии/
#    - assets/input/стадии/factors/
    - assets/input/стадии/проектирование/
    - assets/input/стадии/проектирование/Организация_строительства/
    - assets/input/стадии/проектирование/Оценка_стоимости_ввода_в_эксплуатацию/
    - assets/input/стадии/проектирование/Оценка_стоимости_предпроектного_этапа/
    - assets/input/стадии/проектирование/Предпроектная_подготовка/
    - assets/input/стадии/проектирование/Проектирование/
    - assets/input/стадии/проектирование/Стоимость_исполнительной_документации/
    - assets/input/стадии/проектирование/Стоимость_разработки_проектной_документации/
    - assets/input/стадии/проектирование/Стоимость_разработки_рабочей_документации/
    - assets/input/стадии/проектирование/Стоимость_управления_проектом/
    - assets/input/стадии/строительство/
    - assets/input/стадии/строительство/Внутренние инженерные системы/
    - assets/input/стадии/строительство/Внутренние инженерные системы/Механические системы/
    - assets/input/стадии/строительство/Внутренние инженерные системы/Электрические системы/
    - assets/input/стадии/строительство/Внутренние инженерные системы/Слаботочные системы/
    - assets/input/стадии/строительство/Конструктив/
    - assets/input/стадии/строительство/Нулевой цикл/
    - assets/input/стадии/строительство/Отделка/
    - assets/input/стадии/строительство/Фасады и кровля/
    - assets/input/стадии/строительство/Прочие работы и затраты/
    - assets/calculator_data/factors/
